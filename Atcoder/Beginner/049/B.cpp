#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


int main() {
    int H,W;
    cin >> H >> W;
    vector<string> img(H);
    for(int i=0;i<H;i++) cin >> img[i];
    for(int i=0;i<H;i++) {
        cout << img[i] << endl;
        cout << img[i] << endl;
    }
}